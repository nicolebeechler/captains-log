const express = require('express')
const PORT = process.env.PORT || 3000

const app = express()

// Index Route

// New Route

// Create Route

// Show Route

app.listen(PORT, () => {
  console.log(`${PORT} is working`)
})